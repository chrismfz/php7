diff --git a/ext/dom/php_dom.c b/ext/dom/php_dom.c
index e10f209..db6fe60 100644
--- a/ext/dom/php_dom.c
+++ b/ext/dom/php_dom.c
@@ -1390,6 +1390,14 @@ void dom_normalize (xmlNodePtr nodep)
 						break;
 					}
 				}
+				strContent = xmlNodeGetContent(child);
+				if (*strContent == '\0') {
+					nextp = child->next;
+					xmlUnlinkNode(child);
+					php_libxml_node_free_resource(child);
+					child = nextp;
+					continue;
+				}
 				break;
 			case XML_ELEMENT_NODE:
 				dom_normalize (child);
