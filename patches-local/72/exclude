# Series entries skipped on this base, with reasons (importer never touches this).

# CVE-2017-9118: its 2nd Zend/zend_string.h hunk deletes an old `#define
# ZSTR_MAX_LEN` absent from pristine 7.2.34 (only CloudLinux's newer base has it),
# so it rejects. Kept via patches-local/72/php-7.3-CVE-2017-9118.patch (adds
# ZSTR_MAX_OVERHEAD + ZSTR_MAX_LEN, plus both ext/pcre hunks). Other CVEs use it.
php-7.3-CVE-2017-9118.patch
